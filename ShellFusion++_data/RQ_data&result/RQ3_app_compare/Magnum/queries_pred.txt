-xdev File
set -x
seq Number Number
ssh -p Regex
echo Regex
find Path -name Regex -prune -or \( ! -name Regex \) -print
env Program Program
read -r Regex
finger | awk Program
yes | tr Regex Regex
md5sum File
diff File File
find Path Path -type f -exec chmod Permission {} \;
find Path -type f -exec file {} \; | awk -F Regex Program
tee File
finger Regex
mv File
mkdir -p Directory
-S Path
Regex
find Path
-print
w | awk Program | sed Program | cut -f Number -d Regex | sort | uniq -c
ls -t -r File | head -n -Quantity | xargs -I {} rm {}
set -x
dig Regex
find Path -type d -name Regex -ctime +Timespan -print
who -m | awk Program
gzip -d -c Regex
awk Program Program
echo Regex | gunzip Regex
gzip
basename Regex
bind Regex
pstree -p Regex | sed Program | grep Regex
sed Program File
echo Regex | tr Regex Regex
echo Regex | sed Program
cd $( find Path
rsync -a -v File File
find Path -type f -exec file {} \; -printf '%s\n' | awk -F Regex Program
dig Regex Regex
su Regex
man Regex
find Path -type f -exec file {} \; | awk -F Regex Program
ssh Regex
tee File
rename Regex Regex
curl Regex Regex | sh
ssh -L Regex
find Path -type f -name Regex
rsync -r -f File File File
kill - Regex Regex
rm File
mount -t ntfs
du -c File
find Path -type d
echo Regex | sed Program
cp --help
hostname
bind Regex
mv File File
chown Regex File
find Path -type d
basename Regex
-xdev File
find Path -type f -name Regex | awk Program | bash
bind Regex
set Regex Regex
grep Regex File | wc -l
yes
set Regex
sed Program File
echo Regex | sed -n Program
tee File
top
chown Regex File
find Path -type f -exec file {} \; | awk -F Regex Program
rsync -a -v --exclude Regex File
clear
set -x
cp -a File
mount -o loop
readlink -f File
scp
dig Regex
-ctime -Timespan
set -x
find Path -type d -maxdepth Quantity
find Path -type f \( -name Regex -or -name Regex \) -exec tar -r -f File {} \;
history Quantity
ln -f File Regex
find Path -xdev -name Regex
who
ssh -n Regex touch File
clear
hostname
find Path -type l -exec cp {} File \; -exec readlink {} \;
rm File
ln File
dig Regex Regex
tee File
curl Regex Regex | bash
date --date Regex
set Regex
hostname
echo Regex | md5
diff File
find Path -type f -exec file {} \; -exec echo Regex {} \;
ssh Regex
set Regex
tee File
ssh Regex command
set -x
echo Regex | grep -E -o Regex | head -n Quantity
rm File
echo Regex | awk Program
-S Path
ln -s -f File Regex
echo $( cat File )
echo Regex | tr Regex Regex
ssh Regex
set Regex
cd
watch -n Quantity
du -s File | awk Program
su Regex
md5sum File
rev File
set Regex
seq Number | tr Regex Regex
who -b
tee File
ssh -i File Regex
dig Regex Regex @resolver1.opendns.com
sed Program File
set -x
set Regex
dig Regex -x Regex
chmod -R Permission File
find Path --version
echo Regex | rev
set Regex
mount | grep -o Regex
yes
set Regex Regex Regex
less File
echo $( seq Number )
true | sleep Timespan
ssh -f Regex -p Regex
echo Regex | ssh Regex
cd Directory
chown Regex File
rename Regex
set
dig Regex
curl Regex Regex Regex
mount | grep -o Regex
find Path -mtime +Timespan -exec rm {} \;
su Regex
tee
crontab -l
ssh Regex
chown Regex
echo Regex | tee
-f File
date +%Y:%m:%d -d Regex
set Regex
ping -c Quantity -n Regex | tail -n Quantity | awk Program
touch File File
echo Regex | tr Regex Regex
sort File File
bzip2 File
find Path -type f -exec file {} \; | awk Program
ssh -X Regex
df File | grep Regex
touch File
echo Regex | od -c
echo -n Regex | find Path -type f -exec file {} \; | awk Program
echo Regex | grep -E -o Regex
set Regex
su Regex
set -x | grep Regex
set -x
bind Regex
set Regex
find Path -exec rm {} \;
md5sum File
touch File
rename Regex Regex
set -x
ssh -i File Regex
cp File File
-P | grep Regex
groups Regex | tr Regex Regex
tee File
echo Regex | sed -n Program
read Regex
date
chmod Permission File File
who -b
cp -R File File
date
echo Regex | tee
date
head -n Quantity File File
groups | tr Regex Regex
sleep Timespan
whoami
echo Regex | tr Regex Regex
date -d Regex +%s
basename Regex
date
echo Regex | awk Program
dirname $( which Regex )
echo Regex | tee --append File
history | awk Program
echo Regex | gunzip -t Regex | less
echo Regex | ssh-keygen -t rsa
du -a File | awk Program
du -a File | sort -n -r | head - Quantity
find Path
set Regex
env Regex Program
cat File | sort | uniq
echo Regex | cut -d Regex -f Number
curl Regex Regex | tr Regex Regex
ps -e -f
history Quantity
uname -n
kill - Regex Regex
find Path -type f -iwholename Regex
true | grep Regex
true
echo Regex | awk -F Regex Program
hostname
rmdir Directory
echo Regex | grep -v Regex
head -c Quantity File | grep Regex
nl File
comm -2 -3 File File
find Path -mtime +Timespan -delete
echo Regex | sed -n Program
mkdir -p Directory
ssh Regex -L Regex
echo Regex | cut -d Regex -f Number
split -l Quantity File
ifconfig Regex Regex
set -x
cd Directory
rsync -a -v File File
history | awk Program
$( which Regex )
cp File File
find Path --version
chmod +Permission File
dig Regex Regex @resolver1.opendns.com
rsync -r File File
source <( sort File )
seq Number Number | xargs -I {} mv -t Directory {}
set Regex Regex
ssh Regex command
rmdir
find Path -maxdepth Quantity
find Path -type d -exec ls -l -d {} \;
echo Regex | awk Program
echo Regex | awk Program
grep -i Regex File
sleep Timespan
find Path $( pwd ) -type f | xargs -I {} sed -i Program {}
groups
find Path -maxdepth Quantity -type f -delete
cp -r File File
sort File | uniq | wc -l
rm -r -f File
find Path -name Regex -exec echo {} \; -exec grep Regex {} \;
kill - Regex Regex
echo -e Regex
find Path -type f -printf '%T@ %p\n' | sort -n | tail - Quantity | cut -f Number -d Regex
find Path -name Regex
cd
top
echo Regex | tee -a File
jobs -l
find Path -type f -exec rename Regex {} \;
crontab -l
set -x
ssh Regex
tr Regex Regex
zcat Regex
cat File | xargs -n Quantity -I {} grep Regex {}
find Path -type s
find Path -daystart -ctime Timespan -type f
echo Regex | ssh Regex
echo Regex | ssh Regex
od -t fD File
history | awk Program
cat File
find Path -type f -name Regex -exec sed Program {} \;
sort --random-sort File
rsync -r -t File File
mount | wc -l
mount Regex
find Path Path
man Regex
bg Regex
sort File -o File
mkdir -p $( dirname Regex )
mkdir Directory
echo Regex | awk Program
kill - Regex
gzip -l Regex
bzip2 File
set Regex
chmod Permission File File
find Path -name Regex
find Path -type f -print0 | xargs -0 -I {} mv {} File
md5sum File
date
groups Regex
tar -t File File | cut -f Number -d Regex
rsync -u -t -r File File
groups
find Path -type f -perm +Permission
dig Regex Regex
-xdev File
bzip2 -c File
mkdir -p Directory
top
find Path -type f -name Regex -exec cp {} File \;
paste File
hostname -I
clear
cp -a File File
hostname -I | awk -F Regex Program
find Path -type f -name Regex -exec cp {} File \;
$( whoami )
chmod Permission File
find Path -type f ! -iname Regex -delete
find Path -type f -newermt Regex ! -newermt Regex -delete
set -x
pstree -p Regex
tail -f Regex
chmod Permission File
ps -e -f | grep Regex | grep -v Regex
set Regex Regex
cat File
clear
uname -r
cp File File
curl Regex Regex
sort -o File File
diff File File | grep Regex
curl Regex Regex | sed Program
echo Regex | awk Program Program
cp File File
find Path -type f -name Regex -exec echo {} \; -exec readlink {} \;
kill - Regex Regex
rsync -a -v
ssh -f Regex -p Regex
echo Regex | tr Regex Regex
find Path -name Regex -type f
set -v
ln File Regex
echo Regex | tar -x -z -f File
echo Regex | grep -o Regex
ps -e -o rss=,args= | sort -n -r
grep -v Regex File | xargs -I {} md5sum {}
bzip2 -x
mv File
find Path -type f -wholename Regex
top
groups | tr Regex Regex
cp File File
sed Program File
grep -o Regex File | wc -l
ssh -f Regex -p Regex
curl Regex Regex -C
echo Regex | tr Regex Regex
echo Regex | grep -E -o Regex | tr Regex Regex
mv File
find Path -name Regex
vim $( which Regex )
cd $( whoami )
df File | awk Program
dig Regex Regex
groups Regex | tr Regex Regex
$( which Regex )
cd Directory
kill - Regex $( whoami )
less
find Path -type f -exec chmod Permission {} \;
dig -x Regex Regex
cat File | md5sum
scp -i File
find Path -type f -size Size -delete
dig -x Regex
echo Regex | tee -a File
less
find Path -type f -exec rm {} \;
groups
find Path -name Regex
find Path -type d -empty
find Path -type f -exec ls -s {} \;
find Path -type d -maxdepth Quantity
top
basename Regex
rm -f File File
tr Regex Regex
echo Regex | md5
which Regex
find Path -type f -name Regex -exec mv {} File \; -print
dig Regex Regex
-p -v
find Path -name Regex -exec ls -l {} \;
pstree -p Regex
who | awk Program
who | awk Program
readlink -f File
touch File
find Path -type f -exec cp --parents {} File \;
diff File File
ping Regex
ssh Regex command
curl Regex Regex
vim File
find Path -exec file {} \;
less File
find Path -type d -name Regex -exec rm -r -f {} \;
ssh -L Regex
ssh Regex
set
gzip Regex
rmdir Directory
chown Regex File
sort File -o File
cd
curl Regex Regex | sh
man Regex
rm File
find Path -type f -print0 | xargs -0 -I {} mv {} File
top
df File | grep Regex
echo Regex | ssh Regex
env Program Program
clear
ps -e -o rss=,args= | sort -n -r
groups Regex | tr Regex Regex
rsync -a -v
find Path -type f \( -name Regex -or -name Regex \) -exec tar -r -f File {} \;
rsync -r File File
echo Regex | sed -n Program
readlink -f File
-f File
